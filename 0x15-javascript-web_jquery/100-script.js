document.addEventListener('DOMContentLoaded', function () {
  // When the DOM loads, select the <header> element
  const header = document.querySelector('header');
  //  update the text color to red
  header.style.color = '#FF0000';
});
